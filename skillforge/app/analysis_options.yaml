# Use the curated Flutter lint set. We intentionally don't pile on extra
# style lints (e.g. prefer_const_constructors) so `flutter analyze` stays a
# signal for real problems rather than stylistic noise.
include: package:flutter_lints/flutter.yaml
